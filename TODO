TODO

6. finish code generation

  - after nodes transmit -> on_transmit hook

5. get metrics (manual done -> redo after code changes/size is off)
  - xbee: 1) #frames
          2) bytes sent
  - mcu:  3) time IDS code
          4) code size (LoC + hex bytes)
  scope:
    1) no IDS
    2) IDS HB
    3)     reputation
    4)     HB + reputation

4. write thesis (+ add metrics to IEEE-style article)

  - add section on development of embedded systems
    - compilation/deployment style:
      - hex + install
      - OS process + deployment OTAP
    - target platform definition
  - add section on TinyOS (comparison code inlining)
  - make sure illustrations use same font-sizes, colors, ...
  - node-capture: replace ISP programming with JTAG programming

3. clean up coordinate.py
2. solder motes
1. prepare demo + presentation